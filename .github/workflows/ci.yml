name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: lint · typecheck · build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: install
        working-directory: joaovitrineportfolio
        run: npm ci

      - name: lint
        working-directory: joaovitrineportfolio
        run: npm run lint

      - name: typecheck
        working-directory: joaovitrineportfolio
        run: npm run typecheck

      - name: build
        working-directory: joaovitrineportfolio
        run: npm run build
        env:
          NEXT_TELEMETRY_DISABLED: "1"
