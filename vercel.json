{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build"
    }
  ],
  "routes": [
    {
      "src": "/[^.]+", // Redireciona todas as rotas para o index.html (SPA)
      "dest": "/index.html"
    }
  ]
}